<form method='post' action='<%=url%>'>
    <div class="field">
        <label class="label">Nome</label>
        <div class="control">
            <input class="input" type="text" name="checklist[name]" placeholder="Lista de Tarefas"
                value='<%=checklist.name%>'>
            <% if (checklist.hasOwnProperty('errors')){%>
                <p class="is-danger help">Não deixe o espaço em branco.</p>
            <%} %>
        </div>
    </div>
    <button type="submit" class="button is-success">Salvar</button>
    <a href="/checklists" class="button">Cancelar</a>
</form>